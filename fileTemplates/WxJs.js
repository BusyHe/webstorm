/**
 * File: ${NAME}
 * Created by ${USER} on ${DATE}.
 * Email: busyhe@qq.com
 * Description: ${DESCRIPTION}
 */
const Page = require( '@z-miniprogram/client-lib/page' );

Page({
    \$needLogin: false,
    \$configPath: '$configPath',
    \$alias: '$configPath',
    \$data: {},
    \$onLoad() {
    },
    \$onShow() {
    },
    \$initData() {
    },
    \$onReady: function () {
    }
});
